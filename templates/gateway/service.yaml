apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.gateway.host }}
spec:
  ports:
    - name: "{{ .Values.gateway.port }}"
      port: {{ .Values.gateway.port }}
      targetPort: {{ .Values.gateway.port }}
  selector:
    app: gateway
